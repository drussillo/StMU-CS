/* C program by Dave Russillo, made on 09/10/2024 for CS1310. Checks which quadrant coordinates are in. */
#include <stdio.h>

void main(void) {
    double x;
    double y;
    
    printf("Input: x = ");
    scanf("%lf", &x);
    scanf("");
    printf("\nInput: y = ");
    scanf("%lf", &y);
    printf("\nYou entered point P = (%lf, %lf). \n", x, y);
        
}
